export interface IData {
  id: string;
  name: string;
  targetTemperature: number;
  available: boolean;
}

export interface IFormInput {
  name: string;
  description: string;
  targetTemperature: number;
}
